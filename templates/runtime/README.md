# Harness runtime templates

These templates are owned by the harness. They reference the canonical project policy but do not copy it, install runtime assets, or contain credentials.

Gentle-AI remains the operational authority: use its official install and sync commands for managed runtime assets.
